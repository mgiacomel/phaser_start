{
  "name": "tree-game",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview"
  },
  "dependencies": {
    "phaser": "4.0.0-rc.5",
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vite": "^7.1.12"
  }
}
